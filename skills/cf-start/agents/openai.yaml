interface:
  display_name: "Cflow:start"
  short_description: "Assess, checkpoint, resume, and run refactor work"
  default_prompt: "Use $cf-start to assess or resume Cflow cleanup work in this repository."
policy:
  allow_implicit_invocation: true
