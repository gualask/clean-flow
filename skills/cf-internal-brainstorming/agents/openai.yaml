interface:
  display_name: "Cflow:internal brainstorming"
  short_description: "Resolve user steering after assessment before execution"
  default_prompt: "Use $cf-internal-brainstorming to resolve the open refactor decision before execution."
policy:
  allow_implicit_invocation: true
