interface:
  display_name: "Cflow:architecture"
  short_description: "Build or refresh .cflow/architecture.md from repository state"
  default_prompt: "Use $cf-architecture to build or refresh the Cflow architecture context."
policy:
  allow_implicit_invocation: true
