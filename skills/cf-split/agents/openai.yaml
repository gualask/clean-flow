interface:
  display_name: "Cflow:split"
  short_description: "Evaluate or execute one local behavior-preserving file split"
  default_prompt: "Use $cf-split to evaluate or perform a scoped file-level split."
policy:
  allow_implicit_invocation: true
