interface:
  display_name: "Cflow:review"
  short_description: "Check changes against structure and documented intent"
  default_prompt: "Use $cf-review to check the uncommitted changes before I commit them."
policy:
  allow_implicit_invocation: true
